<!-- 登陆页面 -->
<view class="myLogin">
  <block wx:if="{{isLogin}}">
    <image src="{{avatarUrl}}"></image>
    <text>{{nickName}}</text>
  </block>
  <button wx:else bindtap="getMyInfo">未登录，点此登录</button>
</view>
